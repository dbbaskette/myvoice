export function PacksPage(): JSX.Element {
  return (
    <div className="p-8 text-slate-300">
      <h1 className="text-xl font-semibold text-slate-100">Packs</h1>
      <p className="mt-2 text-sm text-slate-400">
        Pick a pack from the sidebar, or click <strong>+ New pack</strong> to create one. (UI wires
        up in subsequent tasks.)
      </p>
    </div>
  );
}
